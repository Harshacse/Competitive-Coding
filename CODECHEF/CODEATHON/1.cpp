#include <iostream>
using namespace std;
#include <vector>
#define cint(a) int a; cin>>a;

int main()
{
	cint(t);
	while(t--)
	{
		cint(n);
		std::vector<int> v(n,0);
		for(int i=0;i<n;i++)
			cin>>v[i];
		int count = 0;
		for(i=0;i<n;i++)
		{
			
		}
	}
}
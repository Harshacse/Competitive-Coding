#include<iostream>
#include<math.h>
#include<string.h>
using namespace std;
#define ll long long int
char s[100005];
int main()
{
	cin>>s;
	int length = strlen(s);
	int rev[length+1] = {0},str[length+1] = {0};
	int start = (int)"A",end = (int)"Z";
	str[0] = end-s[0]
	for(i=0;i<n;i++)
	{
		str[i]=
	}
}